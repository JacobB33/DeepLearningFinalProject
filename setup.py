from setuptools import setup, find_packages

setup(
    name='deep_brain_scan',
    version='0.0.1',
    description='Deep Brain Scan',
    author='Jacob Berg',
    packages=find_packages(),
)        
